[build]
builder = "NIXPACKS"
buildCommand = "corepack enable && pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm --filter @tomeet/intelligence-worker start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

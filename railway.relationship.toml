[build]
builder = "NIXPACKS"
buildCommand = "corepack pnpm install --frozen-lockfile && corepack pnpm --filter @tomeet/relationship-worker... typecheck"
watchPatterns = [
  "/apps/relationship-worker/**",
  "/packages/**",
  "/package.json",
  "/pnpm-lock.yaml",
  "/pnpm-workspace.yaml",
  "/tsconfig.base.json",
  "/railway.relationship.toml",
]

[deploy]
startCommand = "corepack pnpm --filter @tomeet/relationship-worker start"
healthcheckPath = "/ready"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
